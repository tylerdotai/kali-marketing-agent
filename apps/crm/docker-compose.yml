version: '3.8'

services:
  crm:
    build: .
    ports:
      - "8765:8765"
    volumes:
      # Persist database across restarts
      - ./kali_crm.db:/app/kali_crm.db
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
